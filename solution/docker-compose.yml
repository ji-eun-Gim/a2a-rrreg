version: "3.9"
services:
  app:
    build: .
    env_file:
      - .env
    environment:
      - FLASK_ENV=production
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "3000:3000"
